<% if @building.start.nil? %>
  	<div>
  		<%= render 'form_start' %>
<% elsif @building.end.nil? %>
  <p>
    <strong>Start:</strong>
    <%= @building.start.in_time_zone.to_s(:custom) %>
  </p>
  <p>
  	<div>
  		<%= render 'form_end' %>
  	</div>

<% else %>
  <p>
    <strong>Start:</strong>
    <%= @building.start.in_time_zone.to_s(:custom) %>
  </p>
  <p>
    <strong>End:</strong>
  	<%= @building.end.in_time_zone.to_s(:custom) %>
  </p>
  <p>
    <div>
      <%= render 'form_reopen' %>
    </div>
  </p>
<% end %>
