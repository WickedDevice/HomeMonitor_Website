<%= form_for(@building, html: {:onsubmit => "return confirm('Are you sure you want to end the building?\\n\\nThis will not prevent currently active sensors from reporting any data they have already collected.');" }) do |f| %>
 <% if @building.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@building.errors.count, "error") %> prohibited this building from being saved:</h2>

      <ul>
      <% @building.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :end, :value => "now"%>
  </div>

  <div class="actions">
    <%= f.submit "Stop Monitoring" %>
  </div>

<% end %>
