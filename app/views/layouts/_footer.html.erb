<footer class="footer">
	<row>
		<div class="col-xs-2 pull-left">
		<% if current_user.nil? %>
			<%= link_to "Login", { :controller => "sessions", :action => "new" } %>
		<% else %>
			<%= link_to "Logout", { :controller => "sessions", :action => :destroy }, :disabled => current_user.nil? %>
		<% end %>
		</div>


		<div class="col-xs-2 pull-right"><small>
			<a href="http://wickeddevice.com">Wicked Device</a>
		</small></div>

	</row>

	<% if current_user && current_user.admin? %>
		<row class="nav">
			<ul class="col-sm-6">
				<li>Admin links:</li>
				<li><%= link_to "Sensor Data", sensor_data_path %></li>

				<li><%= link_to "Users", users_path %></li>

				<li><%= link_to "Pages", pages_path %></li> 
			</ul>
		</row>
	<% end %>

</footer>